const config = {}

config.serverPort = 8080;


config.dbConnection = {
    host:'localhost',
    user:'root',
    password:'root',
    port: '3306',
    database:'eventat'
};

module.exports = config;